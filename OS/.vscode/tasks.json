{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make clean && make"
        },
        {
            "label": "clean_without_log",
            "type": "shell",
            "command": "make clean_without_log"
        }
    ]
}